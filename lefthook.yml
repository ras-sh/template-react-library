pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css}"
      run: pnpm dlx ultracite fix {staged_files}
      stage_fixed: true
    typecheck:
      run: pnpm check-types
